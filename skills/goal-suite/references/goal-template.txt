Run a complete repo-enhancement cycle. Strictly phased. All outputs to
.goal-suite/. Apply karpathy-guidelines (no speculation; surgical).

PHASE 1 - DISCOVERY (Subagent: explore):
1. Stack: per references/stacks.md -> STACK.md (BUILD/TEST/LINT/
   FORMAT). stack-override.txt wins.
2. Baseline: BUILD/TEST/LINT/FORMAT exit codes + counts -> BASELINE.md.
   Output MUST hit the transcript.
3. Issues: grep -rn TODO|FIXME|HACK|XXX|DEPRECATED (excl. node_modules
   /target/dist/.venv/vendor/build). gh issue list if gh.
4. Dead-code stack-appropriate (knip / cargo-udeps / ruff F401 /
   staticcheck / phpstan). Tool missing -> note in SPEC.
5. Perf anti-patterns (sync-in-async, await-in-loop, N+1, panic/
   unwrap, blocking-in-event-loop).
6. SPEC.md sections: Stack, Commands, Baseline, Bugs, Lint, Dead-Code,
   Performance, Deprecated. Each finding: file:line + severity
   (CRITICAL/HIGH/MEDIUM/LOW) + needs-research: true|false.

PHASE 2 - RESEARCH (Subagent: external, gated):
7. Skip if --no-research OR no SPEC finding has needs-research:true.
   HARD cap 5 lookups. Source-mandatory. Scope: (a) direct deps vs
   CVE/EOL/major-version-lag, (b) Deprecated-block replacements vs
   current docs, (c) idiom check for flagged anti-patterns. Output ->
   RESEARCH.md (per finding: source URL, confidence, recommendation).
   Research INFORMS the plan; never acts.

PHASE 3 - PLANNING (Superpowers: writing-plans):
8. From SPEC.md + RESEARCH.md, emit writing-plans-conform tasks to
   PLAN.md. Mandatory line:
   - [ ] [SEVERITY] T-N: desc (file:line) -- verify: <cmd>
   2-5 min/task. Order: CRITICAL > HIGH > Lint > Dead-Code > MEDIUM
   perf > Format. NO auto-commits; Phase 6 is external.
   severity=<level> -> filter PLAN. scope=<glob> -> restrict tasks.

PHASE 4 - EXECUTION (Fork-Agents: prefer-/fan-out-fork-agents):
9. Iterate open [ ]. Bug-tasks: systematic-debugging + TDD (failing
   test FIRST). Other: minimal surgical change. Independent parallel
   tasks: fan-out forks with isolation: worktree (soft cap 6). No
   fork recursion.
10. Run task-local verify; output to transcript.
11. Pass: [x] + emit [x]/[ ]/[?] counts via grep -c. Fail: up to 3
    attempts, then [?] with the open question. Continue.
12. Repeat until all [x] or [?].

PHASE 5 - VERIFICATION (Subagent: review, two-stage):
13. Two-stage review of diff since BASELINE.md:
    A (spec): every PLAN task delivered? regression vs BASELINE?
    B (quality): CLAUDE.md compliance if present; bug patterns
    (off-by-one, null-deref, race, panic/unwrap); test coverage vs
    baseline; anti-patterns; secrets / hard-coded URLs.
    Output -> code-review.md with severity + file:line.
14. CRITICAL -> mini-tasks in PLAN.md; non-CRITICAL -> [?] follow-ups.
15. Final BUILD/TEST/LINT/FORMAT in transcript. Then echo CRITICAL
    count: grep -ciE 'CRITICAL' .goal-suite/code-review.md -- MUST be
    0. Apply verification-before-completion: no pass-claim without
    the command's fresh output in THIS message.

PHASE 6 - COMMIT (external): scripts/run-chain.sh or human commit.
Never inside the goal.

GOAL COMPLETE WHEN ALL OF:
- SPEC.md, PLAN.md populated
- RESEARCH.md exists OR research skipped per rule
- All PLAN tasks [x] or [?] (with justification)
- BUILD 0; TEST 0 AND pass-count >= baseline; LINT 0 OR warning-count
  <= baseline; FORMAT 0
- code-review.md CRITICAL count == 0 (echoed via grep -c)

SCOPE CONSTRAINTS (HARD):
- No test deletion / .skip / .only; pass-count never < baseline
- No suppressions without inline justification
- No changes to: .env*, .github/, ci/, deploy/, infra/, terraform/,
  Dockerfile*, migrations/, package.json/Cargo.toml major bumps
- Pure-refactor: business logic unchanged
- Ambiguity -> [?] with the question; do NOT guess
- Research findings without source URL: low-confidence; no destructive
  use

STOP after 40 turns with a report.
